# patterns/ —— 页面模式规范

这一层规定「一个真实页面该怎么组织信息架构」:区域怎么拆、顺序怎么排、
何时用 modal / drawer、metadata 放哪、空状态长什么样、批量操作栏怎么出现……
很多「草稿感」不是颜色问题,而是这一层缺失。

> ⚠ **反应式生长,别批量预写。**
> 真正做某类页面时,才按 `_template.md` 新建一份 `<name>.md`(如 `issue-list.md`、`settings-section.md`)。

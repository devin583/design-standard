# components/ —— 组件规范(规则,不是实现)

这一层规定每个控件的**用途 / 类型 / 状态 / 规则 / token / 正反例**,
但**不写框架实现**(不写 Button.tsx)。目的是约束 AI 怎么用控件,而不是发布组件库。

> ⚠ **反应式生长,别批量预写。**
> 第一次真正用到某个组件时,才按 `_template.md` 为它新建一份 `<name>.md`。
> 凭空预写 15 个规范 = 文档负债,且很可能写错。

新增方式:复制 `_template.md` → 重命名为 `button.md` / `table.md` 等 → 填写。
